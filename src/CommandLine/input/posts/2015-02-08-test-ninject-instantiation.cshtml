@{
    Model.Metadata = 
        new Post
        {
            Title = "Testing dependency resolution",
            Published = DateTime.Parse("2015-02-08"),
            Tags = "ninject,mstest,mvc,controller,dependency,injection".Split(','),
            Categories = ["software"],
        };
}
$markdown$
@Raw(await Model.ReadAssociatedFileAsync(".markdown"))
$/markdown$